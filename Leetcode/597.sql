# Write your MySQL query statement below
SELECT ROUND( IFNULL((SELECT COUNT(requester_id) FROM (SELECT DISTINCT requester_id, accepter_id FROM RequestAccepted) AS T2) / (SELECT COUNT(sender_id) FROM (SELECT DISTINCT sender_id, send_to_id FROM FriendRequest) AS T1), 0), 2) AS accept_rate;